/**
 * Ponyfill entry point that patches `module.register` with the ponyfill
 * implementation. Import this module for its side effect:
 *
 * ```js
 * import 'module-register-ponyfill/ponyfill';
 * ```
 *
 * After importing, `module.register()` uses the ponyfill.
 */
export {};
